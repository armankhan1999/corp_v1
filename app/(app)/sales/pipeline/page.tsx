import { PipelinePage } from "@/components/domain/sales/PipelinePage";

export const dynamic = "force-dynamic";

/** E3-S8 — pipeline board with automatic ageing, drag and keyboard move. */
export default function Page() {
  return <PipelinePage />;
}
