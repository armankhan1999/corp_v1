import { QuotationsListPage } from "@/components/domain/sales/QuotationsListPage";

export const dynamic = "force-dynamic";

/** Quotation list — one of the six nav routes the 63-screen inventory omitted (PLAN.md C-15). */
export default function Page() {
  return <QuotationsListPage />;
}
