import { EnquiriesPage } from "@/components/domain/sales/EnquiriesPage";

export const dynamic = "force-dynamic";

/** E3-S3 — enquiry capture, unassigned queue and saved views. */
export default function Page() {
  return <EnquiriesPage />;
}
