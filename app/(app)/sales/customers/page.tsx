import { CustomersPage } from "@/components/domain/sales/CustomersPage";

export const dynamic = "force-dynamic";

/** E3-S1 — customer, site and contact masters, branch-scoped. */
export default function Page() {
  return <CustomersPage />;
}
